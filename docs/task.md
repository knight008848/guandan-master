# 任务清单 - 掼蛋大师 (TODO List)

为了将当前的开源项目完美对齐我们设计的 PRD 与系统架构，以下是接下来的开发任务清单：

## 🚀 已完成事项 (Completed)
- [x] 将 PRD 文档 ([prd.md](file:///c:/Users/xx/.gemini/antigravity-ide/scratch/guandan-master/docs/prd.md)) 归档至代码仓库中
- [x] 将架构设计文档 ([architecture.md](file:///c:/Users/xx/.gemini/antigravity-ide/scratch/guandan-master/docs/architecture.md)) 归档至代码仓库中
- [x] 配置 GitHub Pages 部署路径基准并编写 GitHub Actions 自动部署流水线
- [x] **严格进贡规则限制**：
  - 修改 `src/session.ts` 中的进贡逻辑。玩家进贡时，计算玩家手牌中最大点数的非逢人配卡牌，仅允许玩家在最高点数对应的卡牌中选择（多张同点数可选不同花色），杜绝投机性选择小牌进贡。
- [x] **级牌（逢人配）同花顺及多组合校验与提示修复**：
  - 重构了 `src/ai.ts` 中的 `extractCardGroups` 逻辑，在手牌组提取阶段深度支持了逢人配替代同花顺、单顺、双顺、钢板、三带二、对子的组合判定，完美解决“级牌没法配同花顺”和“提示功能不可用”的 Bug。
- [x] **配牌（替配）显示功能**：
  - 在出牌区渲染方法中增加了替配检测，出牌时自动在桌面上将红心逢人配替换为被替代卡牌的真实点数和花色（带有“代”字角标和专属青色微光），清楚展示配牌结果，解决“配牌功能没法用”的问题。
- [x] **整理手牌交互增强与双模式切换**：
  - 整理手牌按钮现在支持“按牌值整理”和“按同花/花色整理”的双模式切换，并支持重新渲染手牌时自动保持当前已选中卡牌的选中状态（解决排序会导致选中丢失的痛点），杜绝出错牌问题。
- [x] **局终结算阻碍问题修复**：
  - 在重新发牌阶段的 `deal_started` 事件中，自动关闭并隐藏结算遮罩弹窗（`settlement-overlay`），彻底解决“完成一局后提示框不消失、无法开始下一局”的严重阻塞。

---

## 🛠 核心规则校验严谨化 (Rules & Tribute Constraints)

---

## 🤖 AI 智能分级机制 (AI Levels)
- [ ] **AI 行为模型分级**：
  - 在 `src/ai.ts` 中实现：
    - **初级 AI**：策略偏简单，有大牌就出，优先出单牌/对子，不防守。
    - **中级 AI**：即当前的决策树（保持炸弹和顺子完整度，配合队友接风，对方剩牌少时用炸弹拦截）。
    - **高级 AI**：能够记忆并推算场上已出的大牌、王牌数量，并基于剩余卡牌分布做深度防守或反击。
- [ ] **难度选择 UI 界面**：
  - 在顶栏或游戏主界面中增加难度选择控件（初级/中级/高级），并将其状态同步给 `GameSession` 和 AI 决策模块。

---

## 🎵 音效与多媒体体验 (Audio & UX)
- [ ] **音效文件导入与配置**：
  - 寻找或录制适合的卡牌音效（发牌声、出牌声、PASS声、炸弹爆炸声、结算背景音乐）。
- [ ] **音效引擎实现**：
  - 在 `src/renderer.ts` 中利用 `Web Audio API` 或 `HTMLAudioElement` 在对应的事件发生时（如 `deal_card`、`cards_played`、`pass_played`、`round_ended`）播放对应的音效。
- [ ] **静音开关**：
  - 在 UI 界面添加一个音量/静音切换按钮。

---

## 🎨 牌面与背景个性化配置 (Card Skins & Background Themes)
- [ ] **牌面与牌背图案修改与自定义**：
  - 设计卡牌资源接口，支持切换不同牌面样式（如现代矢量、经典扑克底纹）和不同牌背图案（如墨绿尊贵、中国红、科幻流光）。
- [ ] **游戏背景皮肤切换**：
  - 在 UI 中提供背景选项菜单，允许玩家在不同的牌桌背景（如经典绿呢绒、高档胡桃木纹、深邃星空毛玻璃）之间进行动态切换，实时更新 CSS 变量或图片背景。

---

## 🛡 代码质量与工程化规范 (Code Quality & Linting)
- [ ] **集成 ESLint 与 Prettier**：
  - 配置 ESLint（基于 `typescript-eslint`）对 TypeScript 进行静态校验，排查多余变量与隐式漏洞。
  - 配置 Prettier 规范代码缩进与符号，确保协作时的 Git 提交无格式干扰。
- [ ] **集成 Stylelint 样式校验**：
  - 引入 Stylelint 校验 `src/style.css`，杜绝冗余样式与不兼容属性定义。
- [ ] **配置 Git Hooks 提交校验 (Husky & lint-staged)**：
  - 配置 Git 挂钩，在 `commit` 时仅对暂存区变动文件进行 Lint 修复，确保入库代码 100% 规范。
