# 掼蛋大师 (Guandan Master) 🃏

掼蛋大师是一款运行在浏览器端的精品掼蛋（Guandan）棋牌游戏。项目采用现代感十足的**毛玻璃（Glassmorphism）视觉设计**、高分辨率纯 CSS/SVG 扑克卡牌样式以及细腻的 CSS/JS 动画音效联动，为玩家在电脑端提供真实的沉浸式对局体验。

本项目由 **HTML5 + Vanilla CSS + TypeScript** 基于 **Vite** 构建。核心引擎逻辑与渲染表现完全解耦，具备高度可移植性与可维护性。

---

## 🌟 项目亮点与特色

### 1. 极致精美的视觉设计
- **拟物化绿色呢面牌桌**：采用高拟真径向渐变，附带柔和的内阴影，使出牌区和牌局视感非常饱满。
- **高清现代感卡牌**：利用纯 CSS 打造高清卡牌边框、自适应字号以及圆润阴影。
- **逢人配流光特效**：作为红心主牌的“逢人配”拥有独特的金色外发光和“配”字角标，在理牌时醒目呈现。
- **玻璃态半透明 UI**：游戏顶栏、操作控制区、结算面板和进贡对话框均采用毛玻璃效果。

### 2. 动感流畅的物理动效
- **发牌动效**：卡牌以抛物线轨迹、旋转及缩放效果从牌桌中央依次飞入每个玩家的牌区，伴有流畅的物理惯性。
- **炸弹爆炸粒子特效**：打出炸弹（或同花顺、天王炸）时，触发整个牌桌**屏幕轻微震动**，并从出牌中心喷射出数十个色彩斑斓的粒子光斑，呈扇形消逝。
- **出牌滑行动效**：出牌或过牌（PASS）均伴有滑行进入出牌堆的缓动动画。

### 3. 完整的掼蛋规则链条
- **进贡与退贡**：严格遵循掼蛋规则。局前自动判定是否抗贡（双王抗贡），并支持玩家自主点击卡牌进贡、接收退贡；AI 队友及对手具备完备的自动进贡和退贡挑选机制。
- **逢人配（万能牌）智能拆解**：自动支持红心主牌作为逢人配，配合玩家选中的任意牌型，穷举得出能够组合的最高优先级牌型（如填补顺子中的空缺、配对、配炸弹等）。
- **接风规则**：当某玩家出完最后一张牌成为本轮最大且所有对手 PASS 时，其队友自动获得下一轮的“接风首发权”。
- **晋级与段位攀爬**：支持从打 2 升级到打 A 的积分机制。根据获胜顺序（双游 +3 级，头游+三游 +2 级，头游+末游 +1 级）进行晋级升级。

### 4. 舒适的玩家交互
- **鼠标滑动多选**：支持在手牌区按住鼠标左键横向划过，一次性多选/取消选择多张卡牌，操作极其丝滑。
- **智能提示**：根据上家的牌型和点数，智能在玩家手牌中定位能够压制的最优组合，循环展示。
- **快速整理与重置**：一键重置选择，或以“大王 > 小王 > 主牌 > A > ... > 2”的方式一键排选手牌。

---

## 📂 项目结构描述

```bash
guandan-master/
│
├── docs/            # 设计与开发文档 (PRD、系统架构、任务清单)
├── src/             # 源码目录
│   ├── main.ts      # 应用程序入口
│   ├── session.ts   # 游戏核心状态机 (Finite State Machine / GameSession)
│   ├── rules.ts     # 规则判定引擎 (大小对比、逢人配穷举、牌型识别)
│   ├── ai.ts        # AI 对战决策模块 (托管与对战决策)
│   ├── renderer.ts  # DOM 视觉渲染与交互控制层
│   ├── style.css    # 全局样式，包含色彩系统、卡牌排布及粒子动效
│   └── types.ts     # 游戏公共类型定义
│
├── tests/           # 单元测试目录 (覆盖判定规则、AI行为与对局状态流转)
├── index.html       # 游戏页面骨架
├── package.json     # 项目配置与脚本命令
├── tsconfig.json    # TypeScript 配置文件
└── vite.config.ts   # Vite 构建配置文件
```

---

## 🎮 启动与运行指引

本项目使用了 ES Modules 模块化开发，浏览器无法直接通过双击本地 `index.html` 运行（由于跨域安全限制及 `.ts` 文件解析限制）。请按照以下命令启动本地服务：

### 1. 启动本地开发服务
首先请确保本地已安装 [Node.js](https://nodejs.org/)。在项目根目录下依次执行：
```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```
启动后在浏览器中访问控制台输出的地址（通常是 `http://localhost:5173/guandan-master/`）即可畅玩。

### 2. 运行单元测试
项目使用 Vitest 进行自动化规则与状态机测试：
```bash
# 运行单次测试
npm run test

# 启动测试监听模式
npm run test:watch
```

### 3. 项目构建打包
当需要编译出生产环境静态资源时执行：
```bash
npm run build
```
打包后生成的静态资源将输出至 `dist` 目录中。

---

## 🕹 交互操作说明：
- **选牌**：点击任意卡牌即可使其向上浮起（选中）；再次点击可降下。
- **滑动多选**：按住鼠标左键，横向拖动滑过一排卡牌，可以直接批量选中。
- **托管对局**：点击玩家头像旁的“托管对局”按钮，AI 将接管手牌（自动出牌与自动进退贡），直到本局结束，下一局开始时恢复手动。
- **操作面板**：
  - `出牌`：打出当前选中的牌（系统会自动检测是否符合大过上家的条件）。
  - `不出`：跳过当前回合（上家不是自己时可用）。
  - `提示`：自动在您手牌中寻找可以压制的组合。
  - `重置选择`：取消所有卡牌的悬浮选定。
  - `整理手牌`：支持按“牌值”或“同花/花色”进行手牌整理。
